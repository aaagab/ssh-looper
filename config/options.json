{
    "clear": {
        "info": "Clear previous ssh-looper connections"
    }, 
    "cmd": {
        "args": "resolve,unknown",
        "values": "ssh_cmd"
    },
    "examples": {},
    "resolve": {
        "info": "Resolve the network if dns ip is provided",
        "nested": true,
        "values": "DNS_IP"
    },
    "unknown": {
        "info": "Unknown host When selected following options are added -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null",
        "nested": true
    }
}